# Audit-Report — 2026-07-01

**Ebene 1 (statische Analyse) abgeschlossen.** Ebene 2 (Playwright-Flow-Tests)
steht aus und benötigt deine Entscheidung zu den Scope- und Test-Daten-Fragen
am Ende dieses Dokuments.

## Zusammenfassung

| Kategorie | Wert | Bewertung |
|---|---|---|
| TypeScript-Compile (`tsgo --noEmit`) | 0 Fehler | ✅ |
| Supabase-Linter | **1 ERROR + 109 WARN** | ⚠️ |
| Edge Functions | 105 aktive | ⚠️ (große Angriffsfläche) |
| Ungeschützte `company_name`/`contact_name`-Zugriffe | 139 Stellen in ~30 Dateien | ❌ (siehe Bug #1) |
| `as any` / `@ts-ignore` (Top-Datei) | 24 in `SmartInquiryEditor.tsx` | ⚠️ |
| `toast.error` / `console.error` gesamt | 455 Aufrufe | i.O. (Fehlerpfade sind abgedeckt) |
| Edge-Function-Logs (7d) | Nur "shutdown/boot" sichtbar, kein sichtbarer Fehler-Peak | ✅ (aber selektiv) |

---

## Bug-Kategorien (aus Statik ableitbar, ohne UI-Klick)

### #1 — "null" / "n/a" als Anzeigetext (bestätigt, System-weit)
**Status:** ❌ betrifft alle Kunden-/Firmenanzeigen im Admin
**Symptom:** Karten, Listen und E-Mails zeigen wörtlich `null` oder `n/a` statt
eines Namens. Bereits behoben in `KanbanView.tsx`, aber das Muster wiederholt
sich in **mindestens 25 weiteren Dateien**:

```text
src/components/admin/CateringOrderDetail.tsx:243        {order.company_name && ...}
src/components/admin/CateringOrdersManager.tsx:253,570,659
src/components/admin/EventInquiriesManager.tsx:383-385
src/components/admin/refine/CateringOrderEditor.tsx:800
src/components/admin/refine/EventsList.tsx:633-637
src/components/admin/refine/OrdersList.tsx:272,474
src/components/admin/refine/OrdersKanbanView.tsx:310
src/components/admin/refine/CommandPalette.tsx:158,205
src/components/admin/refine/TasksWidget.tsx:133-139
src/components/admin/refine/InquiryEditor/SmartInquiryEditor.tsx:1085,1175
src/components/admin/refine/InquiryEditor/InquiryDetailsPanel.tsx:179
src/pages/public-offer/CostAcceptanceSection.tsx:73 (B2C-Detection kaputt bei "null")
... +12 weitere
```

Alle prüfen mit `if (x.company_name)` — was für den String `"null"` **truthy**
ist. Konsequenz: falsche B2C-Detection in Cost-Acceptance, falsche
Bilingual-Templates, "null" im Kanban, im Command-Palette, in E-Mail-Vorschau,
PDF-Renderer (`fetchPrintData.ts:121`).

**Fix-Aufwand:** 1 zentrale Helper-Funktion `displayCustomerName()` +
30-Minuten-Sweep durch die Fundstellen.

### #2 — Datenbank-Security-Findings (110 Stück)
**Status:** ⚠️ 1 ERROR, 109 WARN

| Typ | Anzahl | Handlungsbedarf |
|---|---|---|
| **Security Definer View** (ERROR) | 1 | Sofort prüfen — View umgeht RLS |
| Function Search Path Mutable | ~15 | Migration `SET search_path = public` |
| Extension in Public | 2 | Info, meist unkritisch |
| RLS Policy Always True (INSERT/UPDATE/DELETE) | ~10 | ❗ potentielle Datenlecks |
| Auth OTP Long Expiry | 1 | Auth-Config |
| Leaked Password Protection Disabled | 1 | Auth-Config, 1 Klick |
| Signed-In Users Execute SECURITY DEFINER | ~80 | Meist gewollt, aber ungeprüft |

**Kritisch (sofort ansehen):** die 1 Error-View und die ~10 RLS-Policies mit
`USING (true)` für schreibende Operationen — hier könnte jeder eingeloggte User
fremde Daten ändern.

### #3 — `SmartInquiryEditor.tsx` als Risiko-Hotspot
- **24 `as any` / `@ts-ignore`** — höchster Wert im Projekt
- **26 `toast.error`-Aufrufe** — zweithöchster Wert
- **139-Zugriffe auf `contact_name`/`company_name`** kommen mit dieser Datei

Diese Datei ist statistisch der wahrscheinlichste Ort für "unerklärbare Bugs".
Empfehlung: gezielter Playwright-Durchlauf `Anfrage öffnen → alle Tabs klicken
→ speichern` als **erster** Ebene-2-Test.

### #4 — `useOfferBuilder.ts` — 33 Fehler-Pfade
Höchste `toast.error`-Dichte im Projekt (33). Das ist **nicht per se ein Bug**,
aber jeder dieser Fehlerpfade sollte in Ebene 2 einmal ausgelöst werden
(fehlendes Package, ungültige Menge, doppelte Option), um zu sehen, ob die
Fehlermeldung auf Deutsch, sinnvoll und ohne UI-Freeze erscheint.

### #5 — 105 Edge Functions ohne zentrale Health-Übersicht
`create-lexoffice-*` allein 4 Funktionen, `create-*-payment*` 6 Funktionen,
`create-*-checkout` 3 Funktionen — es gibt vermutlich doppelten Code und
Drift. Für Ebene 2 nur die tatsächlich vom Happy-Path aufgerufenen Functions
testen (via Network-Tab beobachten, welche wirklich feuern).

---

## Top-10 konkret verdächtige Stellen (aus Statik)

| # | Datei / Zeile | Verdacht |
|---|---|---|
| 1 | `CostAcceptanceSection.tsx:73` | `isB2C = !inquiry.company_name` — bei `"null"` false → B2B-Cost-Acceptance für Privatkunden |
| 2 | `SmartInquiryEditor.tsx:1085,1175` | Firma-Chip zeigt `"null"` im Header |
| 3 | `TasksWidget.tsx:133-139` | Task-Liste zeigt Firma "null" |
| 4 | `CommandPalette.tsx:158,205` | Command-Palette-Suchergebnis "null" |
| 5 | `EventInquiriesManager.tsx:384` | Anfragen-Liste zeigt Firma "null" |
| 6 | `CateringOrdersManager.tsx:253` | Print-HTML enthält Firma "null" |
| 7 | `fetchPrintData.ts:121` | PDF-Renderer: `companyName: inq.company_name` roh → PDF mit "null" |
| 8 | `AIComposer.tsx:71-72` | AI-Prompt bekommt Firma `"null"` als Kontext → beeinflusst Text-Generierung |
| 9 | `FinalizePanel.tsx:63-64` | Angebots-Finalisierung übergibt "null" an E-Mail-Renderer |
| 10 | DB-Linter ERROR (Security Definer View) | Umgeht RLS — unbekannt welche Daten sichtbar werden |

---

## Ebene 2 — Ablaufplan (wenn du "OK" sagst)

Playwright-Runs in folgender Reihenfolge, je 1 Datei pro Flow unter
`docs/audit/e2e/`:

```text
P0-1  Anfrage per Kontaktformular → landet in Kanban → Notification
P0-2  Angebot bauen (alle 3 Modi) → speichern → Vorschau
P0-3  "Vorschau anzeigen" → Public-Offer öffnet mit Preview-Mode
P0-4  Kunde akzeptiert Option → Stripe-Checkout Redirect
P0-5  Restzahlungs-Link erzeugen → E-Mail-Preview → Kunden-Ansicht
P0-6  Gutschein kaufen (Frontend) + einlösen (Admin)
```

Ergebnis pro Flow: eine Zeile Status + Screenshot + Console-/Network-Log.

---

## Deine Entscheidungen für den nächsten Schritt

1. **Zentrale `displayCustomerName()`-Helper einbauen + 25 Fundstellen fixen**
   sofort (30 Min) — bevor Ebene 2 läuft, weil sonst jeder Playwright-Run
   dieselben "null"-Screenshots produziert?
2. **Scope Ebene 2**: nur Admin + Public-Offer + Payments, oder auch die
   öffentliche Website mit einbeziehen?
3. **Test-Daten**: darf ich in Ebene 2 Test-Anfragen anlegen (Test-Mode-Flag
   ist im UI vorhanden), damit ich Webhook + LexOffice-Sync validieren kann?
4. **DB-Security-Findings**: soll ich die 1 ERROR-View und die 10 "RLS Always
   True"-Policies parallel angehen (eigene Migration), oder erst Ebene 2 fahren?

---

## Ebene 1 Wave 1+2 — DB-Security-Fixes (erledigt 2026-07-01)

- ✅ ERROR behoben: `inquiry_offer_history` View auf `security_invoker=true`
- ✅ 4 redundante `service_role`-Policies mit `USING (true)` entfernt
- ✅ 2 zu weite `authenticated`-Policies auf `inbox_emails` entfernt (Mandanten-Isolation greift)
- ✅ `search_path` für `custom_access_token_hook` + `is_current_tenant` fixiert
- ✅ Leaked Password Protection (HIBP) aktiviert; Signups deaktiviert

**Linter jetzt: 106 WARN, 0 ERROR** (vorher 1 ERROR + 109 WARN)

Verbleibende Warnungen sind gewollt (öffentliche Kontaktformulare, `pg_trgm` in
public, öffentliche SECURITY DEFINER Functions wie `get_public_offer`) oder
auth-config, die nur im Backend-UI abschaltbar ist (OTP long expiry).

---

## Ebene 2 P0-1 — Anfrage-Flow (E2E, 2026-07-01)

**Status: ✅ funktioniert** — mit 3 Findings

**Repro:** `/tmp/browser/p0-1/e2e.py` — füllt `/events/#kontaktformular`,
submittet, prüft Response + Success-Toast + DB-Record + Notification-Flag.

**Ergebnis:**
- HTTP 200 von `receive-event-inquiry`, `inquiryId` zurückgegeben
- Success-Toast erscheint ("innerhalb von 24 Stunden")
- DB-Record angelegt (`v2_events` via Compat-View)
- `notification_sent = true` → E-Mail an Admin ging raus, WhatsApp fire-and-forget getriggert
- Test-Datensatz nachträglich als `is_test=true, archived=true` markiert

**Findings:**

1. **Kein Test-Mode-Flag auf öffentlichem Formular** (P1)
   Der User kann von außen nicht als Test markieren. Jede E2E-Anfrage landet
   erst als echte Anfrage im Inbox+WhatsApp-Alarm und muss danach manuell
   ausgeblendet werden. Fix: `EventContactForm` sollte bei Marker `E2E-TEST-`
   im Firmenname automatisch `isTest: true` an die Function senden — oder
   Query-Param `?test=1` respektieren.

2. **`Firma` ist Pflichtfeld — Privatkunden können nicht anfragen** (P0)
   `z.string().min(2, "Firmenname erforderlich")` in `EventContactForm.tsx:35`.
   Konsequenz: Privatpersonen tippen "Privat", "-", "n/a" → das ist genau die
   Quelle für die "null"-Anzeige-Kaskade aus Bug #1. Fix: Firma optional
   machen ODER B2C/B2B-Toggle einbauen.

3. **Console-Warnungen auf `/events/`** (P2, kosmetisch)
   - `Each child in a list should have a unique "key" prop` in `Navigation.tsx`
   - `React does not recognize the "fetchPriority" prop` auf einem `img`

Keine tatsächlichen Runtime-Errors, keine Netzwerk-Fehler, keine
Fehlermeldungen an den User.

---

## Empfehlung nächster Schritt

**P0 aus P0-1 zuerst fixen** (Firma optional machen), weil er die Wurzel der
ganzen "null/n/a"-Anzeige-Kaskade ist, die wir in Wave "null-Fix" schon
System-weit patchen mussten. Danach P0-2 (OfferBuilder Playwright-Run).