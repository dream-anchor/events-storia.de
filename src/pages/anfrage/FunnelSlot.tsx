import EventContactForm from "@/components/events/EventContactForm";

/**
 * Ursprünglich der Slot für den mehrstufigen Anfrage-Funnel (FunnelShell,
 * am 29.06.2026 als verwaist entfernt). Layout/Slot-Position 1:1 original,
 * Formular-Embed ersetzt durch das MAESTRO-Widget (wie auf /kontakt).
 */
export const FunnelSlot = () => <EventContactForm />;
